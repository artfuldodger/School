<%= form_for([@link, @link_comment]) do |f| %>
  <% if @link_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link_comment.errors.count, "error") %> prohibited this link_comment from being saved:</h2>

      <ul>
      <% @link_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
